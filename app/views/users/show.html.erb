<div id="user_show">
  <!--User Header-->
  <div class="jumbotron">
    <div class="container">
      <h1><%= @user.first_name %></h1>
    </div>
  </div>
  <!--end User Header-->

  <div class="container">
    <div class="col-md-4">
      <h2>Profile</h2>
      <p>
        <%= image_tag @user.img, :class => "profile_image" %>
        <br>
        First Name: <%= @user.first_name %>
        <br>
        Last Name: <%= @user.last_name %>
        <br>
        Current City: <%= @user.current_city %>
        <br>
        Joined: <%= @user.created_at %>
      </p>
      <%= link_to "Edit", edit_user_path(@user), class: "btn btn-primary" %>
      <%= link_to "Add Itinerary", new_itinerary_path, class: "btn btn-primary" %>
    </div>
    <div class="col-md-8" style="overflow:scroll; height:400px;">
      <!--********LOOP THROUGH USER ITINS****************-->
      <% @user.itineraries.each do |itinerary| %>
        <div id="itin_name" class="row">
          <h2><%= link_to "#{itinerary.name}", itinerary_path(itinerary.id) %>, </h2><p>(<%= itinerary.duration %> days)</p>
        </div>
        <div id="itin_desc" class="row limit">
          <h4>
            <%= itinerary.desc %>
          </h4>
        </div>
        <!--*******LOOP THROUGH ACTIVITIES IN INTIN*******-->
        <div id="scroller-wrapper">
        <div id="container">
          <% itinerary.activities.each do |activities| %>
            <div class="contents" id="pic"><%= image_tag activities.img, :class => "act_image" %><br><%=activities.name%></div>
          <% end %>
        </div>
        </div>
      <% end %>
      <!--*******CLOSE ACTIVITIES LOOP*******-->
      <!--********CLOSE ITINERARY LOOP****************-->
    </div> <!--closes col-md-8-->
  </div> <!--closes container-->
</div> <!--closes user_show-->
